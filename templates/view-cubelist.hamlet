<h1>#{cname}

<ul>
    $forall (E.Value card, carddetails) <- cs
        <li>
            #{card} #
            $case carddetails
                $of Nothing
                    name not found!
                $of Just (Entity _ _)
                    found!
