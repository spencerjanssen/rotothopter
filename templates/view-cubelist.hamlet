<h1>#{cname}

<ul>
    $forall (card, carddetails) <- cs
        <li>
            #{card} #
            $case carddetails
                $of Nothing
                    name not found!
                $of Just (Entity _ _)
                    found!
